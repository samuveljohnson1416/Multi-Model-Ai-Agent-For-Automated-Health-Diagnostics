# Blood Report Analysis System - Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# LLM Provider Configuration
# ===========================================

# Primary: Hugging Face Inference API (used when Ollama is not available)
# Get your token from: https://huggingface.co/settings/tokens
HF_API_TOKEN=your_huggingface_token_here

# Hugging Face Model to use (default: mistralai/Mistral-7B-Instruct-v0.2)
HF_MODEL_ID=mistralai/Mistral-7B-Instruct-v0.2

# Alternative HF models you can use:
# - mistralai/Mistral-7B-Instruct-v0.3
# - meta-llama/Llama-2-7b-chat-hf
# - google/gemma-7b-it

# ===========================================
# Local Ollama Configuration (Optional)
# ===========================================

# Ollama server URL (default: http://localhost:11434)
OLLAMA_URL=http://localhost:11434

# Ollama model name (default: mistral:instruct)
OLLAMA_MODEL=mistral:instruct

# ===========================================
# LLM Provider Priority
# ===========================================

# Options: "ollama_first", "hf_first", "ollama_only", "hf_only"
# - ollama_first: Try Ollama first, fallback to HF API (default for local)
# - hf_first: Try HF API first, fallback to Ollama
# - ollama_only: Only use Ollama (fails if not available)
# - hf_only: Only use HF API (for cloud deployment)
LLM_PROVIDER_PRIORITY=ollama_first

# ===========================================
# OCR Provider Configuration
# ===========================================

# Option 1: OCR.space API (RECOMMENDED - Free: 500 requests/day)
# Get your FREE key at: https://ocr.space/ocrapi/freekey
# No credit card required, instant activation
OCR_SPACE_API_KEY=your_ocr_space_key_here

# Option 2: Google Cloud Vision API (Free: 1000 requests/month)
# Requires Google Cloud account with billing enabled
# Get key at: https://console.cloud.google.com/apis/credentials
GOOGLE_VISION_API_KEY=your_google_vision_key_here

# Option 3: Hugging Face OCR (uses HF_API_TOKEN above)
# Model for OCR (default: microsoft/trocr-base-printed)
HF_OCR_MODEL=microsoft/trocr-base-printed

# ===========================================
# OCR Provider Priority
# ===========================================

# Options: "tesseract_first", "api_first", "tesseract_only", "api_only"
# - tesseract_first: Try local Tesseract first, fallback to APIs (default)
# - api_first: Try cloud APIs first, fallback to Tesseract
# - tesseract_only: Only use local Tesseract
# - api_only: Only use cloud APIs (for HF Spaces deployment)
OCR_PROVIDER_PRIORITY=tesseract_first

# ===========================================
# Application Settings
# ===========================================

# Enable debug mode (shows detailed logs)
DEBUG_MODE=false

# Request timeout in seconds
LLM_TIMEOUT=30
OCR_TIMEOUT=30

# Max retries for API calls
LLM_MAX_RETRIES=3
