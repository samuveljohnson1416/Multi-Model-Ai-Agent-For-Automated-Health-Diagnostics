# Shell scripts must keep LF endings: a CRLF shebang ("bash") makes the container fail with
# "exec ./start-*.sh: no such file or directory". Windows checkouts would otherwise convert them.
*.sh text eol=lf
